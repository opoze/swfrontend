import { PerfiPipePipe } from './perfi-pipe.pipe';

describe('PerfiPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PerfiPipePipe();
    expect(pipe).toBeTruthy();
  });
});
